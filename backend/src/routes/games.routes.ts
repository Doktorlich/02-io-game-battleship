import { Router } from "express";

const router = Router();

// router.post("/");
// router.get("/available");
// router.post("/:id/join");
// router.get("/:id");
// router.delete("/:id");
// router.post("/:id/surrender");

export default router;
