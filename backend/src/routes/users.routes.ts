import { Router } from "express";

const router = Router();

// router.get("/:id");
// router.get("/me");
// router.get("/leaderboard");
// router.get("/:id/stats");
// router.get("/:id/history");

export default router;
